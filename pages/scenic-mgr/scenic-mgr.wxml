<view class="manager-container">
  <view class="header">
    <text class="title">??? 景点管理</text>
    <button class="add-btn" bindtap="showAddDialog">+ 添加景点</button>
  </view>

  <view wx:for="{{list}}" wx:key="id" class="item">
    <image src="{{item.image}}" mode="aspectFill"></image>
    <view class="info">
      <text class="name">{{item.name}}</text>
      <text class="city">{{item.city}}</text>
    </view>
    <view class="actions">
      <text class="edit" bindtap="showEditDialog" data-id="{{item.id}}">编辑</text>
      <text class="delete" bindtap="deleteItem" data-id="{{item.id}}">删除</text>
    </view>
  </view>

  <view wx:if="{{list.length === 0}}" class="empty">暂无景点数据</view>
</view>